# =============================================================================
# Agents — Question Bank
# =============================================================================

[[questions]]
text = "What stop_reason terminates the canonical agent loop?"
options = ["max_tokens", "end_turn", "refusal", "tool_use"]
answer = 1

[[questions]]
text = "What is 'compaction' in the context of long-running agents?"
options = [
    "Compressing the response with gzip",
    "Summarizing earlier conversation history to fit the context window",
    "Reducing the number of tools available to the model",
    "Switching from Opus to Haiku to save cost",
]
answer = 1

[[questions]]
text = "Why are sub-agents architecturally useful?"
options = [
    "They reduce API key exposure",
    "They isolate work in a fresh context, bounding parent context growth and enabling specialization",
    "They run on cheaper hardware",
    "They are mandatory for tool use",
]
answer = 1

[[questions]]
text = "The Claude Agent SDK was previously known as:"
options = [
    "The Anthropic SDK",
    "The Claude Code SDK",
    "The MCP SDK",
    "The HHH Framework",
]
answer = 1

[[questions]]
text = "Which production agent built on the Agent SDK is the canonical reference implementation?"
options = ["Claude.ai", "Claude Code", "Claude Console", "Bedrock Console"]
answer = 1

[[questions]]
text = "Which of the following is NOT a typical hook point in a Claude agent loop?"
options = [
    "Pre-call hook",
    "Pre-tool hook",
    "Post-token hook (after every emitted token)",
    "Post-tool hook",
]
answer = 2

[[questions]]
text = "Which agent-loop safeguard is most directly intended to prevent infinite tool-calling?"
options = [
    "max_tokens",
    "Maximum turn count",
    "Temperature 0",
    "API rate limit",
]
answer = 1

[[questions]]
text = "Which language(s) are first-class for the Claude Agent SDK?"
options = [
    "Python and TypeScript",
    "Java only",
    "Rust only",
    "Bash only",
]
answer = 0

[[questions]]
text = "What is the most common architectural reason to drop down from the Agent SDK to the raw Messages API?"
options = [
    "Cost",
    "Highly customized agent topologies that the SDK doesn't fit",
    "Lack of streaming support in the SDK",
    "The SDK doesn't support tool use",
]
answer = 1
